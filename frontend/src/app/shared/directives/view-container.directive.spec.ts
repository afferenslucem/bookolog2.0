import { ViewContainer } from './view-container.directive';

describe('ViewContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewContainer();
    expect(directive).toBeTruthy();
  });
});
