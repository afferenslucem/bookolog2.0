import { PagePadding } from './page-padding.directive';

describe('PagePaddingDirective', () => {
  it('should create an instance', () => {
    const directive = new PagePadding();
    expect(directive).toBeTruthy();
  });
});
